{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo \"$CLAUDE_TOOL_INPUT\" | jq -r '.file_path // empty'); if [[ \"$FILE\" == *.dart ]] && [[ ! \"$FILE\" == *.g.dart ]] && [[ ! \"$FILE\" == *.mocks.dart ]]; then dart format \"$FILE\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
